@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - NET My Photo Album</title>
    
    <!-- FONT AWESOME -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    
    <!-- BOOTSTRAP -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    
    <!-- Custom CSS -->
    <link href="/css/site.css" rel="stylesheet" />

</head>
<body>
    <header>
        <div class="container-custom">
            <nav class="navbar navbar-expand-lg navbar-light">
                <div id="logo">LOGO</div>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse ms-5" id="navbarNav">
                    <ul class="navbar-nav">
                        
                        <li class="nav-item">
                            <a href="@Url.Action("Index", "Guest")" class="nav-link">Home</a>
                        </li>
                        
                        <li class="nav-item">
                            <a href="@Url.Action("Photographs", "Guest")" class="nav-link">Photographs</a>
                        </li>
                    </ul>
                </div>
                 <div class="collapse navbar-collapse" id="navbarNav">
                     <ul class="navbar-nav">
                        <!-- code omitted -->
                        @if (ViewData["Title"]?.ToString() != "Log in" && ViewData["Title"]?.ToString() != "Register")
                        {
                            <partial name="_LoginPartial" />
                        }
                     </ul>
                 </div
            </nav>
        </div>
    </header>
    <div class="wrapper">
        @if (SignInManager.IsSignedIn(User))
        {
            <aside>
                <nav>
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link text-dark" aria-current="page" href="@Url.Action("Index", "Photo")">Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark" aria-current="page" href="@Url.Action("PhotoList", "Photo")">Photos</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark" aria-current="page" href="@Url.Action("Index", "Category")">Categories</a>
                        </li>
                    </ul>
                </nav>
            </aside>
        }
        <main role="main" class="">
            @RenderBody()
        </main>
    </div>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
